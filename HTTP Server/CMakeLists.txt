# CMakeList.txt : CMake project for HTTP_Server, include source and define
# project specific logic here.
#
cmake_minimum_required (VERSION 3.8)

# Add source to this project's executable.
add_executable (HTTP_Server "HTTP_Server.cpp" "HTTP_Server.h")

